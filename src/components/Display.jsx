import React from 'react'; // eslint-disable-line no-unused-vars
import './Display.css'

const Display = (props) => {
  return (
    <div>
      {props.value}
    </div>
  );
}

export default Display;